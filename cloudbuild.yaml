steps:
- name: 'ubuntu'
  entrypoint: 'bash'
  args:
  - '-c'
  - | 
    echo 'Hello World'
    echo $TRIGGER_NAME
    echo $BRANCH_NAME

options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET