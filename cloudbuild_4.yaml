steps:
  - name: "ubuntu"
    script: |
      #!/usr/bin/env bash
      echo "Hello $_NAME"

  - name: "ubuntu"
    script: |
      #!/usr/bin/env bash
      echo "Your project id is: $PROJECT_ID"

substitutions:
  _NAME: "John"

options:
  automapSubstitutions: true
  logging: CLOUD_LOGGING_ONLY